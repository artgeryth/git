# 美术素材来源与授权（Artwork attribution & permission）

## 结论先说

- 本仓库的**代码、台词、文档：MIT**（见 `LICENSE`）
- `content/sprites/` 里的角色立绘：**一张网上流传的 AI 生成图**
  → **没有人类作者可署名，本仓库也不对它主张任何版权**，且它**不在 MIT 授权范围内**，不建议商用
- 本仓库的**发布版带着这张图**（维护者的选择）；下面是必须保留的说明。
  任何人对此有异议，开 Issue 即可，我们会**立即移除或替换**

## 这张图的实际情况

| 项目 | 情况 |
|---|---|
| 来源 | 网传 AI 生成图。原图右上角带 **bilibili 水印**，无法确认真正的生成者是谁 |
| 人类作者 | **无（或不可考）**。AI 生成物通常不构成受版权保护的作品，所以没有"原作者"可写 |
| 本仓库做的处理 | 从三视图裁切出正面/侧面/背面并抠掉白底；**没有重绘，也没有改动角色设计** |
| 版权主张 | 本仓库**不对这张图主张任何权利**，也不保证第三方不主张 |
| ⚠ 商用风险 | 无法确认生成它的 AI 服务条款。**有些服务对免费档输出限制商用**（如 Midjourney 免费档为 CC BY-NC），所以**不要把它当作可商用素材** |
| 异议处理 | 任何人认为这张图与自己有关 → 开 Issue，**立即移除/替换**（删掉 `content/sprites/` 即可，程序会自动改用原创占位形象） |

> 一句话：`LICENSE` 里的 MIT 只保护"这个项目的代码、台词和文档"，
> 这张图既不是本项目的作品，也不能被 MIT 授权出去。

## 为什么可以带着它发布

AI 生成物在多数司法辖区**不构成受版权保护的作品**，因此：

- 不存在"必须署名"的作者（我们仍然如实写明来源不明，而不是宣称是自己画的）
- 本项目没有从它获利、也没有把它纳入任何商业许可（MIT 明确把它排除在外）

需要留意、并且已经按最保守方式处理的点：

1. **生成它的服务条款未知** → 所以明确写"不建议商用"，也不给别人商用授权
2. **发帖者可能主张"这一版具体输出"** → 所以写明来源不明 + 收到异议立即移除
3. **别人 fork 之后拿它去商用** → `LICENSE` 与 `README` 都写明了它不在 MIT 覆盖范围内，
   商用需要自己解决授权（或换成 `content/placeholder/` 的原创形象）

## 想彻底干净？两条路

**① 换成你自己的角色**（最推荐）

把**你自己画的**（或用 AI 生成、且已确认许可范围覆盖你的用途的）三视图放进 `content\sheet\`：

```bat
tools\make-sprites.exe content\sheet\你的三视图.jpg content\sprites "59-376,392-791,809-1226"
tools\build.bat          :: 程序图标会自动用新形象的头像重新生成
tools\make-package.bat   :: 重新打包
```

**② 用仓库自带的原创占位形象**

删掉 `content/sprites/`（或把 `.gitignore` 里那三行的注释取消），程序会自动改用
`content/placeholder/` 里那套**纯代码绘制**的小团子——它是本项目的原创素材，**随 MIT，可自由商用**。

## 给使用 AI 图的额外建议

- 不要从别人发的帖子里"抠掉水印"再当自己的素材：即使图是 AI 生成的，
  发布者可能对"这一版具体输出"主张权益，也可能违反生成它的服务条款。
- 如果以后换成 AI 生成的立绘，建议在 README 里注明**用了哪个工具、哪个档位**，
  并确认该档位允许你的用途（尤其是商用与他人再分发）。
